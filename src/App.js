import {useState} from "react";

const App = () =>{
  const [isHome, setIsHome] = useState(true);
}

return(
  <div>
    <button>Home</button>
    <button>Menu</button>
  </div>
)

export default App;
